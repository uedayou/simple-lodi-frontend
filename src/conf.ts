const DEFAULT_ACCEPT_TYPE: string = "text/turtle";

// for debug
const DEBUG_MODE: boolean = false;
const DEBUG_REPLACE_URL: string = "https://uedayou.net/loa/";
const DEBUG_REPLACE_HOSTNAME: string | null = null;

export {
  DEFAULT_ACCEPT_TYPE,
  DEBUG_MODE,
  DEBUG_REPLACE_URL,
  DEBUG_REPLACE_HOSTNAME,
};
